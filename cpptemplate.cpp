#include <bits/stdc++.h>
#define fr(x,y,z) for(auto x=y;x!=z;x++)
#define er(x, y) cout<<x<<" "<<y
using namespace std;

int main(int argc, char *argv[]){
	int n, ai, p;
	cin >> n;
	fr(i,0,n)cin >> ai;

	er(p, endl);return 0;
}
